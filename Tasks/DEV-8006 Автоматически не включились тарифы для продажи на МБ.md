#task #BBD #PMS

 13.05.2025
[link](https://tracker.yandex.ru/DEV-8006)

**PMS query:**
```sql
SELECT * FROM hotels WHERE id = 30468
```
hotels_providers
```sql
SELECT * FROM hotels_providers WHERE hotel_id = 30468
```
providers
```sql
SELECT providers.* FROM providers  
JOIN hotels_providers AS h_providers ON providers.id = h_providers.provider_id  
WHERE h_providers.hotel_id = 30468
```
**BBD query:**
cms_suppliers_docs:
```sql
SELECT * FROM `cms_suppliers_docs` WHERE lcode = 30468
```
cms_suppliers_bills:
```sql
SELECT bills.* FROM `cms_suppliers_bills` AS bills 
INNER JOIN `cms_suppliers_docs` AS docs ON docs.id = bills.supplierdoc_id
WHERE docs.lcode = 30468
```
cms_suppliers:
```sql
SELECT suppliers.* FROM `cms_suppliers` AS suppliers
INNER JOIN `cms_suppliers_docs` AS docs ON docs.supplier_id = suppliers.id
WHERE docs.lcode = 30468
LIMIT 1
```
cms_suppliers_docs_balances:
```sql
SELECT balances.* FROM `cms_suppliers_docs_balances` as balances
INNER JOIN `cms_suppliers_docs` AS docs ON docs.id = balances.supplierdoc_id
WHERE docs.lcode = 30468
```
