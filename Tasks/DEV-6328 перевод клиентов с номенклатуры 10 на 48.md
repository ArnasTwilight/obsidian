#task #BBD

 17.03.2025
[link](https://tracker.yandex.ru/DEV-6328)

1. В текущей реализации мы столкнёмся с проблемами в цепочке действий:
   "Комиссия" -> "Выставление счетов за Шлюз оплат 0,5" -> Загрузка файла для создания счёта. 
   - При попытке выставить счёт, если у нас номенклатура не 46 или 47 (новые тарифы), то счёт создан не будет
   - Так же не будет выставлен счёт в случае если нет баланса на договор.
2. Можно хранить в файле (например .json) в директории проекта:
   `$_SERVER['DOCUMENT_ROOT'] . '/application/files/test.json'`
   Иные менее предпочтительные варианты: 
   - В БД
   - Хардкод
3. Нужно реализовать перевод клиента с 10 номенклатуры на 48 путём добавление логики в таск на 18:00. Изменяться будет только номенклатура с шлюзом, все счета остаются неизменными. Хранение списка клиентов для перевода описано в пункте 2.
   При попытке выставить счёт на переведённого клиента через "Комиссия" нужно внести изменение в логику его работы для старых тарифов. Необходимо:
   - Расширить условие для выборки номенклатур (добавить старые номенклатуры)
   - Исключить проверку баланса
   - Выставлять счёт в статусе "Не оплачен"
   
P.S: В текущей реализации из интерфейса BBD "/supplierdocs/index/ID/" мы не можем создать договор на "Шлюз оплат 0,5" (48) без договора на "Bnovo PMS / Комплекс" или "ЧМ + Модуль / Онлайн-продажи"