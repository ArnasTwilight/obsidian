#BBD

[link](https://git.dev.bnovo.ru/internal/bnovo.booking.desk/-/blob/master/app/application/classes/model/bill.php?ref_type=heads)

```php
get_agreement_suffix_ru()
```
Содержит логику по определению договора оферты по номенклатурам и в зависимости от даты заключения

```php
get_name($language = 'ru')
```
Содержит в себе много условий по определению сервиса счёта, для вывода наименования услуги.
p.s: "Есть сам счёт, а есть зафиксированный факт того, что мы его создали, чёт такое, как я понимаю"

```php
save()
```
Выполняет сохранение объекта в БД, но так же после этого выполняет у него очистку файлов:
- public/upload/bills/
- public/upload/acts/

```php
get_upd_docs_list()
```
На текущий момент в этом методе сосредоточена вся основная логика по формированию и привязке УПД к счетам. Является проблемным методом и нуж

```php
/**  
 * По счету должен выдаваться документ УПД вместо обычного акта выполненных работ 
 * Для Биново это правило действует для счетов, дата начала услуг по которым в 2021 году 
 */
is_upd_instead_act(): bool
```
Выполняет проверку, что должно выводиться УПД или акт